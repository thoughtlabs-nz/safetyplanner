CONVEX_URL=http://127.0.0.1:3210
STORAGE_PATH=../../storage
MQTT_URL=mqtts://broker.example.com:8883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_CLIENT_ID=mqtt-ingest
MQTT_TOPIC_PREFIX=ddpai

# Used only by scripts/publish-test-data.ts — must match a camera's ssid
# already registered in Convex (Settings page).
TEST_CAMERA_SSID=vYou_DDPai_MINI
